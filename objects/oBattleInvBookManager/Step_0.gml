var alphaAmount = 0.05; 
var xAddAmount = 7;

if (fadeInDone == false) { fadeInFunc(alphaAmount, xAddAmount); }
if (fadingOut = true) { fadeOutFunc(alphaAmount, xAddAmount); }


if (mouse_wheel_down()) { scrollingBookDown(); }
if (mouse_wheel_up()) { scrollingBookUp(); }

